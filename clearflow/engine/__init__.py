"""ClearFlow logic layer - scheduling, gating, and the pledge ledger.

These modules are pure (no I/O, standard library only) so the keystone-focus
and domain-unlock rules can be exercised in isolation.
"""
